redux toolit notes

in redux we have the following terms 
that is 
first we neeed to configure store
then we make  a slice in which we can initialize the initial state and then we have a reducer function in which we can define the reducers 
reducer basically update that states
in reducer we have two things that is state and action

then we move configure store and in that we put our reducer so that they stores our updated state in our store.
 then we have a two hooks that is useDispatch and use selector
in useDispatch is used for e.g we want to update the states from teh frontend then the useDispatch is used 
it send the request to the store then to the reducer 
the useSlectror is used to get the updated states from the store and then its showed onthe frontend
.

then we have async thunk 
it have three components 
pending 
fulfilled 
rejected 
we mainly use this to fetch our data from our backend and for that it returns teh promise that it first fetch the data from the databse or from backend 
thenit move towards 

we dont use reducer in that 
we use extra reducer in that 
we then a have builder to add our case
then we add our first case 
in first case we have pending case where it make the request to load 
and then in second case where our request is fulfilled 
and then in third addcase we have rejected request if our request is not fulifilled or have some error then it rejected the request 
and gievs the error


then we have have to persist our value in our store or frontend
for that we have to make our changes in the store only
we just set all our reucders in a combined reducer
then we make our config for teh peristing the value and we have key value pairs in that
we have key that is root
then we have storage to store 
then we have a whitelist for which the value needs to be store 
then we use persistreducer medthod in which we setup our persistconfig and persisted reducer
then in our configure store method we define our persistedreducer

