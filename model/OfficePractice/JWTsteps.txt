jwt we have header payload and signature

=>Below all done in login api 

first we will sign the jwt
in which we have user.id user.email
the we have "my sceret key",
then we have {expires in : "15 mins"}

then we send res.json
in which we will send that the user in login and if we want to send credetials we can send them too
e.g
user:user.id user.email etc
and if we want we can send the token too


=> now we create a middleware to check our token for the respected user 
first we take header from the client like req.header.authorization
the header look like this [bearer,token]
we split the header as we only want token 
so we slpit the header like [1]
now we have  a token sended by the user
now we verify this token with my scret token 
and from that we took our user.id and user.email

if it matches the user will login and of not the user will not login


=>
now we create a protected route in which the 

in that we just put a parameter authmiddleware 
as in post method it goes from left to right
if authmiddleware then it will redirect to protected route

=>for a refresh token
we can simply add a refresh token in login page 
then we create a refresh token route
in that we sign a new token to a my secret key
that is signed by a refresh token and it will be valid for the time we provided




